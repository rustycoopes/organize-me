# Environment & secrets
.env
.env.local
.env.*.local
github-sa-key.json
*.json.key
service-account*.json

# Claude Code local settings (machine-specific)
.claude/settings.local.json
# Claude Code worktrees & local plans — full repo copies / scratch; keep them out of
# search (ripgrep/glob respect .gitignore) and out of the tree
.claude/worktrees/
.claude/plans/
.claude/scheduled_tasks.lock

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
.Python

# Packaging / build artifacts
build/
dist/
*.egg-info/
*.egg
wheels/
pip-log.txt
pip-delete-this-directory.txt

# Test / coverage / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json
.coverage
.coverage.*
coverage.xml
*.cover
htmlcov/
.tox/
.nox/

# Celery runtime
celerybeat-schedule
celerybeat.pid

# Jupyter (if used for exploration)
.ipynb_checkpoints/
